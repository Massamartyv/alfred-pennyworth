# Automations

Scripted workflows that live on the Mac. Each automation is a self-contained folder with its own script, dependencies and README.

## Structure

```
Automations/
├── {Automation Name}/
│   ├── {automation-name}.py       -- Primary script (kebab-case, matches folder)
│   ├── requirements.txt           -- Python dependencies
│   └── README.md                  -- Scope, usage, design intent
```

## Current Automations

Verified against the filesystem 2026-09-10 – six folders exist under `Automations/`.

| Automation | Scope | Purpose | Status |
|---|---|---|---|
| Capability Matrix | System | Scans scheduled agents, native crew subagents and scheduled-task registrations; writes a three-table capability matrix into `Agents/_index.md` between HTML markers | Live. Regenerated by the heartbeat on the first of each month; safe to run manually, `--write` is idempotent |
| Guards | System | Two guard instruments: `state-patterns.grep` (the shared pattern file the monthly Drift Audit greps for state-shaped blocks in the wrong place) and `working-sweep.sh` (30-day `.working/` archival sweep) | `state-patterns.grep` is actively consumed by the Drift Audit cadence. **`working-sweep.sh` has never fired** – confirmed 2026-07-23: no `Context/Archive/working-YYYY-MM/` folder exists on disk, and multiple `.working/` subdirectories are over 30 days untouched with nothing archived. The July heartbeat working-directory sweep is outstanding |
| The Almanac | Personal | Renders the personal Notion schedule onto the `Almanac` calendar in Calendar.app as six four-hour blocks with tasks inside them. Notion is the authority; the calendar is a one-way projection. Also serves the Sunday session with `pool` and `sweep` | Live, verified 2026-09-09. Dry run unless `--execute`. Interactive ritual lives in the `almanac` skill |
| The Assay | System | Surface-agnostic evaluation harness – three suites (gates and monitors, skills, Notion writes), one scalar each, one append-only verdict ledger. The measurement layer named as the precondition on opening Tier 1 autonomy | Live, verified 2026-09-10. Autonomy Tier 0 – proposes and measures only, never applies. Gates suite in the pre-commit hook; full run weekly in the Whetstone Retrospective |
| Voice | System | Reads Alfred's responses aloud via the macOS `say` command through a `Stop` hook, gated by an on-demand toggle | Live. Off by default; toggled with "voice on/off" or `./voice` from this folder |

## Conventions

- **Folder name:** Title Case (`Pinterest Organizer/`, `Finance Dashboard/`)
- **Script name:** lowercase kebab-case matching the folder (`pinterest-organizer.py`)
- **Python dependencies:** `requirements.txt` in the automation folder
- **Environment variables:** Read from `~/Alfred Pennyworth/.env` where needed
- **README:** Required. Describes scope (personal vs venture), purpose, usage, design intent

## Adding a New Automation

1. Create `Automations/{Automation Name}/`
2. Add the primary script (kebab-case filename)
3. Add `requirements.txt` with pinned dependencies
4. Add `README.md` following the existing examples
5. If the automation is venture-scoped, document the scope and its plugin/credential routing clearly

---

*Last updated: 2026-09-10 – The Assay built and verified live under Mission The Assay; the folder count trued to six. Previously 2026-09-09 – The Almanac built and verified live; the folder count trued to five. Previously 2026-08-11 – Domesday: Finance Dashboard and Pinterest Organizer deleted by operator ruling; three automations remain. The 1 June voice toggle found armed was cleared the same pass – voice is off by default again.*
